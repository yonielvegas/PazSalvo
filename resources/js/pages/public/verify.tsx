import { Head } from '@inertiajs/react';
import { AlertTriangle, Building2, CheckCircle2, Clock3, FileText, SearchX } from 'lucide-react';
type Certificate = null | { status:'valid'|'expired'|'cancelled'; folio:string; client_number:string; holder_name:string; full_address:string; agency:string; generated_by:string; authorized_by:string|null; issued_at:string; expires_at:string; cancelled_at:string|null; cancel_reason:string|null; pdf_url:string|null };
export default function Verify({certificate:c}:{certificate:Certificate}) {
    const meta = !c ? {icon:<SearchX/>,title:'Certificado no encontrado',text:'No existe un certificado válido asociado a este código.',class:'missing'} : c.status==='cancelled' ? {icon:<AlertTriangle/>,title:'CERTIFICADO ANULADO',text:'Este certificado fue anulado y no debe ser aceptado como válido.',class:'cancelled'} : c.status==='expired' ? {icon:<Clock3/>,title:'CERTIFICADO EXPIRADO',text:'Este certificado expiró y se muestra únicamente como referencia histórica.',class:'expired'} : {icon:<CheckCircle2/>,title:'CERTIFICADO VIGENTE',text:'El certificado es auténtico y se encuentra dentro de su periodo de vigencia.',class:'valid'};
    return <div className="verify-page"><Head title="Verificar certificado"/><header><div className="brand"><Building2/><div><b>AAUD</b><span>Validación pública de certificados</span></div></div></header><main><section className={`verify-card ${meta.class}`}><div className="verify-icon">{meta.icon}</div><h1>{meta.title}</h1><p>{meta.text}</p>{c&&<><dl><div><dt>Folio</dt><dd>{c.folio}</dd></div><div><dt>NAC</dt><dd>{c.client_number}</dd></div><div><dt>Cliente</dt><dd>{c.holder_name}</dd></div>{c.status!=='cancelled'&&<><div><dt>Dirección</dt><dd>{c.full_address}</dd></div><div><dt>Agencia</dt><dd>{c.agency}</dd></div><div><dt>Elaborado por</dt><dd>{c.generated_by}</dd></div><div><dt>Autorizado por</dt><dd>{c.authorized_by || 'No configurado'}</dd></div></>}<div><dt>Fecha de emisión</dt><dd>{new Date(c.issued_at).toLocaleString('es-PA')}</dd></div>{c.status!=='cancelled'&&<div><dt>Fecha de expiración</dt><dd>{new Date(c.expires_at).toLocaleDateString('es-PA')}</dd></div>}{c.cancelled_at&&<div><dt>Fecha de anulación</dt><dd>{new Date(c.cancelled_at).toLocaleString('es-PA')}</dd></div>}{c.cancel_reason&&<div><dt>Motivo</dt><dd>{c.cancel_reason}</dd></div>}</dl>{c.pdf_url&&<a className="verify-pdf" href={c.pdf_url} target="_blank"><FileText/> Ver certificado PDF {c.status==='expired'&&'(referencia histórica)'}</a>}</>}</section></main></div>;
}
